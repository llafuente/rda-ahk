/*!
  class: RDA_AutomationWindowSearch
*/
class RDA_AutomationWindowSearch {
  /*!
    Property: title
      string - Search by title (full match)
  */
  /*!
    Property: $title
      string - Search by title (regex)
  */
  /*!
    Property: process
      string - Search by process (full match)
  */
  /*!
    Property: $process
      string - Search by process (regex)
  */
  /*!
    Property: path
      string - Search by path (full match)
  */
  /*!
    Property: $path
      string - Search by path (regex)
  */
  /*!
    Property: classNN
      string - Search by class name (full match)
  */
  /*!
    Property: $classNN
      string - Search by class name (regex)
  */
}
